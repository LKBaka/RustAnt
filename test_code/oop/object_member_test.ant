class A {
	let val = 0;
	
	#[method]
	let plus = func (self) {
		self.val = self.val + 1
		self.val
	}

	func new() {
		copy(A)
	}
}

let s = now();

let a = A.new();
print(a.plus(1).plus(1))

print(now() - s);