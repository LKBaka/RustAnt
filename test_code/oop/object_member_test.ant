class A {
	let val = 0;
	
	#[method]
	let plus = func (self, n) {
		self.val = self.val + n
		self
	}

	#[method]
	let to_int = func(self) {
		self.val
	}

	func create() {
		copy(A)
	}
}

let a = A.create();

print(a.plus(1).plus(1).to_int());
