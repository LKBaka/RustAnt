func fib(n) {
    return if n < 1 {
        0
    } else if n == 1 {
        1
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

let start = now();

print(fib(1));

let end = now();

print(end - start);

input()