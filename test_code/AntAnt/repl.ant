// use io;

use lexer;
use token;

let Lexer = lexer.Lexer;

//while true {
	let code = "aaa!321";

	let l = Lexer.create(code);

	let r = l.next_token();
	while r != token.eof() {
		print(r.to_string())
		r = l.next_token()
	}
//}